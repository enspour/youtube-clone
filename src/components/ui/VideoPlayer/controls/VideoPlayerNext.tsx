"use client";

import { FC } from "react";

import { Icon } from "../../Icon";

import NextIcon from "@/assets/icons/player/next.svg";

const VideoPlayerNext: FC = () => {
    return <Icon svg={NextIcon} />;
};

export default VideoPlayerNext;
